Les 10 principals Cryptomonnaies : https://api.coinstats.app/public/v1/coins?skip=0&limit=10&currency=EUR
On veux pour chaque crypto :
- Son nom
- Son logo
- Le prix actuel en euro avec 4 deciaml max ( voir pour changer de color quand sa baisse ou monte)

test modif  2
let callBackGetSuccess = function (data) {
    console.log(data.coins[1]);
}


function GetService () {
    var url= "https://api.coinstats.app/public/v1/coins?skip=0&limit=10&currency=EzUR";
    $.get (url, callBackGetSuccess).done(function () {
//alert( "second succesS)
    })
        .fail(function () {
            alert("error");
        })
        .always (function (){
        });
}
//alert ( finished" )

GetService()



for(let user of users) {
      $trombi.append(createDivUser(user));
    }



// Elèment de l'objet
				let id = coins[i].id;
				let rank = coins[i].rank;
				let name = coins[i].name;
				let icon = coins[i].icon;
				let price = coins[i].price;
				let volume = coins[i].volume;
				let priceChange1h = coins[i].priceChange1h;
				let price1h = Math.sign(coins[i].priceChange1h);

				//Construction tableau
				cryptoCoins += '<tr>';
				cryptoCoins += `<td> ${rank} </td>`;
				cryptoCoins += `<td> <img src="${coins[i].icon}"> </img> ${coins[i].name} </td>`;
				cryptoCoins += `<td> ${price} </td>`;
				cryptoCoins += `<td> ${volume} </td>`;
				cryptoCoins += `<td id="price1h${id}" class="ccc"> ${priceChange1h} </td>`;
				cryptoCoins += '</tr>';
				document.getElementById("data").innerHTML = cryptoCoins;
